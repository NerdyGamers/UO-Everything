# UOAssetSuite resources

Place UOFiddler's `Ultima.dll` in this folder as `Ultima.dll` before running a full adapter-backed build or the read-only smoke test. The binary is not committed here because it is sourced from the external UOFiddler project.
